import { ComingSoon } from '@/components/common/coming-soon';

export const metadata = { title: 'Roles & Permissions' };

export default function Page() {
  return <ComingSoon titleKey="nav.item.roles" />;
}
