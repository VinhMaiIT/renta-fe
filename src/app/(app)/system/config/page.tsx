import { ComingSoon } from '@/components/common/coming-soon';

export const metadata = { title: 'System Config' };

export default function Page() {
  return <ComingSoon titleKey="nav.item.systemConfig" />;
}
