import { ComingSoon } from '@/components/common/coming-soon';

export const metadata = { title: 'Catalog' };

export default function Page() {
  return <ComingSoon titleKey="nav.item.categories" />;
}
