import { ComingSoon } from '@/components/common/coming-soon';

export const metadata = { title: 'Payments' };

export default function Page() {
  return <ComingSoon titleKey="nav.item.payments" />;
}
