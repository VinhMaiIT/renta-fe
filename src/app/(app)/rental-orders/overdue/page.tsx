import { ComingSoon } from '@/components/common/coming-soon';

export const metadata = { title: 'Overdue Orders' };

export default function Page() {
  return <ComingSoon titleKey="nav.item.overdueOrders" />;
}
